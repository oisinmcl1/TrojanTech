On new machines the temp file has to be manually created.

I'm very lazy.

Both powershell script and batch file achieve the same thing.

They ensure its run with admin privileges.

Check if 'temp' already exists.

If it doesn't, create 'temp' at the root of C: drive.

Oisín Mc Laughlin
05/07/2024